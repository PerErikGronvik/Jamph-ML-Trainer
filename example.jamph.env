# Example Jamph configuration
# Copy this to jamph.env (lowercase) and fill in your credentials
# jamph.env is gitignored for security

# HuggingFace
HF_USERNAME=your_hf_username
HF_TOKEN=your_huggingface_token_here
### Hugginface token creation guide:
# 1. Go to https://huggingface.co/settings/tokens
# 2. Click "New token"
# 3. Needs write premissions to upload models, and read permissions to download private models
# 4. Copy the token and paste it here (keep it secret!) Do not copy the folder, use git to share. 

# Model naming prefix (used for unique model names)
MODEL_PREFIX=jamph

# Optional: Developer metadata
GITHUB_HANDLE=YourGitHubUsername
TEAM=YourTeamName
ORGANIZATION=YourOrganization
ROLE=YourRole